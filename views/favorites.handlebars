<h1>Favorites List</h1>

    {{#each product}}
    <div class="center">
            <article class="card">
            <h1 class="cardText">{{this.product_name}}</h1>
            <img src="/images/{{this.image}}" class="img" alt="">
            </article>
    </div>
    {{/each}}
